import { ITab } from '../Shared/Interfaces/Main.interface'
import { TableSpace } from '../Shared/Interfaces/Table.interface'
import { MockGenHelpers } from '../Utils/MockGen'

// import { scanRowsData1 } from './TableData'

export const reoTabsData1: ITab[] = [
  {
    id: 'tabs-with-underline-item-1',
    tabIndex: 0,
    label: 'GSM',
    data: MockGenHelpers.generateMockReoTableData(30, [
      TableSpace.IColumnTypes.Enum,
      TableSpace.IColumnTypes.Operator,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
    ]),
  },
  {
    id: 'tabs-with-underline-item-2',
    tabIndex: 1,
    label: 'LTE',
    data: MockGenHelpers.generateMockReoTableData(50, [
      TableSpace.IColumnTypes.Enum,
      TableSpace.IColumnTypes.Operator,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Country,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Text,
    ]),
  },
  {
    id: 'tabs-with-underline-item-3',
    tabIndex: 2,
    label: 'UMTS',
    data: MockGenHelpers.generateMockReoTableData(100, [
      TableSpace.IColumnTypes.Enum,
      TableSpace.IColumnTypes.Operator,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
    ]),
  },
  {
    id: 'tabs-with-underline-item-4',
    tabIndex: 3,
    label: 'WiFi',
    data: MockGenHelpers.generateMockReoTableData(50, [
      TableSpace.IColumnTypes.Enum,
      TableSpace.IColumnTypes.Operator,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Country,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Text,
    ]),
  },
  {
    id: 'tabs-with-underline-item-5',
    tabIndex: 4,
    label: 'Bluetooth',
    data: MockGenHelpers.generateMockReoTableData(100, [
      TableSpace.IColumnTypes.Enum,
      TableSpace.IColumnTypes.Operator,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
    ]),
  },
  {
    id: 'tabs-with-underline-item-6',
    tabIndex: 5,
    label: '5G',
    data: MockGenHelpers.generateMockReoTableData(30, [
      TableSpace.IColumnTypes.Enum,
      TableSpace.IColumnTypes.Operator,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Signal,
      TableSpace.IColumnTypes.Checkbox,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
      TableSpace.IColumnTypes.Text,
    ]),
  },
]
